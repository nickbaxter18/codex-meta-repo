
- Entry point:  initialises FastAPI, CORS, Sentry, database bootstrapping, and router registration.

- main.py initialises FastAPI, configures CORS, sets up Sentry (when DSN supplied), bootstraps the database, seeds data, and mounts routers.

